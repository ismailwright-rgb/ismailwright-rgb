fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.13.4
httpx==0.28.1
pymupdf==1.24.14
pytesseract==0.3.13
pillow==11.0.0
chromadb==1.0.15
# Needed at import time by api/main.py's /transcribe route (FastAPI
# inspects UploadFile parameters when the route is registered, not just
# when a request actually arrives) - so this is a base dependency even
# though the voice feature itself (faster-whisper) stays optional.
python-multipart==0.0.32
